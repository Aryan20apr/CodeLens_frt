# Copy to `.env.local` (or rename) — Next.js loads that automatically.
# Backend API origin (not the Next app). Include http:// or https:// so OAuth navigates correctly.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
